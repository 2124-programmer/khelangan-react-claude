# Copy this file to .env and set your PC's LAN IP.
# Find it with: ipconfig  (look for "IPv4 Address" under your WiFi adapter)
# This URL is baked into the app bundle at build time.
EXPO_PUBLIC_API_URL=http://YOUR_PC_LAN_IP:8080
EXPO_PUBLIC_GOOGLE_MAPS_KEY=
